def Two_pointers(arr):
    n = len(arr)
    left = 0
    right = n - 1 

    while left < right:
        #we have to do some logic according to the questiom
        #we have to do some logic of what to do more among the 3
            #number 1 is left++
            #number 2 is right--
            #or increase both the left and right
            