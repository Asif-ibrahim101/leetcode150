#if we have two arrays to traverse
def Two_arrays_template(arr1, arr2):
    i = j = 0

    while i < arr1.length AND j < arr2.length:
        #Do some logic here depending on the problem
        #o some more logic here to decide on one of the following:
            1. i++
            2. j++
            3. Both i++ and j++
        
        #step make sure both iterables are exhuasted
        while i < arr1.length:
            #do some logic here dependsing on the problem
            i++

        while j < arr2.lengh:
            #do some logic and 
            j++